.DS_Store
node_modules/
*.log
docs/workspace/logs/
work/
outputs/

# 🔴 刻意不跟踪：本仓 governance.yaml truth_source=github，GitHub main 即安装源与自动
# 更新源，推 main 直达真实用户；AGENTS.md 含内部治理路径与生意运营细节，不得进公开仓
# 同步/校验两侧已把本仓登记进 public-exempt 豁免。
# 于是三份指令文件一律不入库：本地形态与其它仓相同（AGENTS.md 全文 + CLAUDE.md 单行
# `@AGENTS.md` 加载器），只是不被 git 跟踪，GitHub 上不再有它们。
# 🔴 它们不再经 git 在两机之间同步——换机/新克隆后要手工补，或从另一台拷。
AGENTS.md
GEMINI.md
CLAUDE.md
